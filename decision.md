# Decision Log（前端修改记录）

> 用途：记录前端原型（screens/）在评审后的修改决定，供后续调整 backend spec（docs/backend spec files/）时对照。
> 格式：每条记录 = 修改内容 + 涉及页面 + 对 backend 的影响。
>
> 版式契约：成长册 widget 网格的几何、像素取整、触控与渲染要求见 `docs/frontend spec files/growth-book-layout-spec.md`（权威来源为后端仓库 `DECISIONS.md` E3 / W1-W21）。本文只记前端改动，不重复那些数值。

---

## 2026-07-31

### 1. 社区共育筛选：从「任务名称」改为「任务类型」

**页面**：`screens/community-coeducation.html`

**修改内容**：
- 筛选器标签由「任务名称」改为「任务类型」，选项由具体任务名（社区建筑里的纹样 / 社区里的食物来源 / 我会安全过街）改为三项：全部任务、日常任务（daily）、社区任务（community）。
- 动态卡片新增 `data-type="daily|community"` 属性，筛选逻辑按类型匹配；原 `data-task`（具体任务名）保留但不再参与筛选。
- 卡片底部任务标注同步改为「日常任务：xxx」「社区任务：xxx」格式。

**对 backend 的影响**：
- 任务类型枚举与 `db_parent_task.parent_task_type`（`t1=daily(日常任务)|t2=community(社区任务)`，见 `05 home-school-spec.md`）一致，前端筛选值可直接复用该枚举。
- `db_community_submission` 目前只有自由文本 `task_label`，没有类型字段，无法支撑按类型筛选。需要二选一：
  - a) 给 `db_community_submission` 增加 `parent_task_id` 外键，关联 `db_parent_task` 后取 `parent_task_type`；
  - b) 直接在 `db_community_submission` 上增加 `task_type` 字段（沿用 t1/t2 枚举）。
- 社区共育列表接口需支持按任务类型 + 时间的组合筛选。

---

## 2026-08-01

### 2. 成长档案「儿童评价」板块重组：教师评价合并为单一入口，新增「教师寄语」

**页面**：`screens/growth-record.html`、`screens/teacher-evaluation.html`（新增）、`screens/teacher-message.html`（新增）、`screens/teacher-monthly-evaluation.html`、`screens/teacher-term-evaluation.html`、`screens/growth-comprehensive-assessment.html`、`screens/growth-book.html`

**修改内容**：

2.1 成长档案首页（growth-record.html）
- 「儿童评价」入口由 5 个（发布家长评价 / 成长册 / 教师月度评价 / 教师学期评价 / 综合评估）合并为 3 个横向卡片：发布家长评价（不变）、教师评价（新的合并入口）、成长册。
- 「孩子评价进度」表结构不变（家长月度/学期、教师月度/学期、综合、成长册六列），但状态由三态（已完成/进行中/未完成）简化为两态：已完成 / 未完成，用颜色 dot 表示。

2.2 新增教师评价中间页（teacher-evaluation.html）
- 上半部分 4 个卡片入口：月度评价、学期评价、综合评估、教师寄语（新功能）。
- 下半部分为本班幼儿进度表，共 4 列：本月评价、学期评估、综合评估、教师寄语；状态不用文字，用颜色 dot 表示完成 / 未完成两态。
- 「本月评价」列不在前端写死月份，由后端根据当前日期自动识别当月并返回该月完成状态（2026-08-01 评审反馈）。

2.3 新增教师寄语页（teacher-message.html）
- 教师可勾选个别幼儿单独撰写寄语，也可「全选」对全班统一撰写。
- 寄语内容仅限文字，300 字以内（前端 maxlength=300 + 字数计数）；不支持图片、视频等附件。
- 幼儿列表上显示每人寄语完成状态（dot）。

2.4 月度 / 学期评价页新增「查看过往评价」入口
- teacher-monthly-evaluation.html：进度表新增「操作」列（查看 / 继续 / 填写），配合原有月份选择器可回看任意月份的评价；已完成的评价以只读方式打开（`?view=1`）。
- teacher-term-evaluation.html：新增「查看学期」选择器（可切换往届学期），进度表同样新增「操作」列。
- 两页返回键由 growth-record.html 改为指向 teacher-evaluation.html（综合评估页同步修改）。

2.5 成长册（growth-book.html）
- 成长册内容删除「体检数据」和「家长动态」；「幼儿评语」替换为「教师寄语」；「发展评估」拆分为「期末评估」和「综合评估」。
- 内容按固定顺序排列：园所介绍 → 在园时光 → 亲子活动 → 期末评估 → 综合评估 → 教师寄语。
- 生成检查表不再用文字状态（完成/缺月评/草稿/待补/缺图/可生成…），改用颜色 dot 表示完成 / 未完成（结果列 dot 表示可生成 / 不可生成）。

**对 backend 的影响**（对照 `05 home-school-spec.md`）：
- 【新增业务对象】教师寄语目前 spec 中不存在，需新增表（建议 `db_teacher_message`）：`message_id, school_id, class_id, child_id, teacher_id, content(max_len=300, 纯文本), publish_status, published_at`；写入接口需支持按勾选的多个 child_id 批量创建（全选=全班），每个幼儿一条记录。
- 【成长册构成变更】成长册组成项由 7 项改为 6 项：删除体检数据（health）、家长动态（parent moments）两个数据来源；新增教师寄语；原「发展评估」在检查表中拆为期末评估 + 综合评估两列。spec 中成长册生成条件 / 检查表相关字段需同步调整，且组成项有固定展示顺序（intro→time→task→term→comp→message）。
- 【状态口径统一】所有进度类表格（成长档案首页、教师评价进度、成长册检查表）一律二元状态（已完成/未完成），与 spec 第 170-172 行「主页只允许 h1/h2」的口径一致并延伸到详情页进度表；「进行中/草稿/待补」等中间态不再对外展示（草稿态仅在月度/学期评价自身页面内部保留）。
- 【查询接口】月度评价需支持按月份查询历史记录（列表 + 单个幼儿详情只读），学期评价需支持按学期查询历史记录；即评价记录需带 month / term 维度且可回溯。
- 【导航结构】教师侧评价路径变为：成长档案 → 教师评价（聚合页）→ 月度/学期/综合/寄语，聚合页需要一个按幼儿返回 4 项完成状态的汇总接口。

### 3. 月度/学期评价「完成情况」统一改版，教师寄语页重排（取代 2.4 的选择器方案）

**页面**：`screens/teacher-monthly-evaluation.html`、`screens/teacher-term-evaluation.html`、`screens/teacher-message.html`

**修改内容**：

3.1 月度评价（teacher-monthly-evaluation.html）
- 底部板块统一命名为「完成情况」（原「当月进度表」）。
- 完成情况改为「幼儿 × 月份」矩阵：月份列由已录入的评价月份自动生成（如已录入 3—6 月则显示 4 列；录入 3—7 月则显示 5 列），不在前端写死。
- 删除「最近更新」列（无意义）；删除 2.4 中加的「查看月份」选择器和「操作」列。
- 状态只有已完成/未完成两种，用颜色 dot 表示。
- 【查看详情方案·已确认】矩阵中每个圆点本身可点击：点击已完成的圆点打开该幼儿该月的评价详情（只读），点击未完成的圆点进入该月填写页。进度表同时充当查看入口，无需单独筛选功能。（2026-08-01 已确认）

3.2 学期评价（teacher-term-evaluation.html）
- 只展示当前学期，完成情况仅 2 列：幼儿、学期评价（dot 两态）；删除 2.4 中加的「查看学期」选择器和「操作」列。
- 查看详情与月度一致：点击已完成圆点查看该幼儿学期评价详情，未完成则进入填写。
- 导出弹层中的「草稿」表述同步改为「未完成」。

3.3 教师寄语（teacher-message.html）
- 页面分两个板块：①寄语内容（顶部单卡片）→ ②完成情况（底部，幼儿 × 寄语完成状态，dot 两态；命名与月度/学期页统一为「完成情况」）。
- 「添加到」是寄语内容卡片的一部分（非独立板块），为下拉框，且位于寄语文本框之前——先选施加对象，再写寄语；下拉选项 = 全体幼儿 + 本班幼儿名单（单选）。
- 完成情况的圆点同样承担查看详情：点击已完成圆点跳转到独立的寄语详情页（`teacher-message-detail.html`，新增），展示该幼儿的寄语全文，并提供「重新编辑」按钮——进入编辑态（300 字限制）后可保存修改或取消，用于纠正错误填写；点击未完成圆点自动在上方「添加到」中选中该幼儿并定位到填写区。

**对 backend 的影响**：
- 月度评价完成情况接口：按班级返回「已存在评价记录的月份列表」+ 每名幼儿在这些月份的二元完成状态；不需要最近更新时间字段。
- 月度评价需支持按 child_id + month 查询单条评价详情（只读回看）。
- 学期评价接口只需返回当前学期数据（本页面不做历史学期回看）；支持按 child_id 查询当前学期评价详情。
- 教师寄语提交接口：一段寄语内容 + 添加目标（全体幼儿 或 单个幼儿，下拉单选）；选全体时为班内每名幼儿各生成一条记录，选个人时仅生成一条；另需按班级返回每名幼儿的寄语完成状态。
- 教师寄语需支持按 child_id 查询已写寄语的内容（寄语详情页），并支持更新已提交的寄语（重新编辑保存，内容仍限 300 字纯文本）。

### 4. 月度/学期评价表单：统一「选择照片」导入过往照片

**页面**：`screens/teacher-monthly-form.html`、`screens/teacher-term-form.html`

**修改内容**：
- 两个表单页交互完全统一：只保留一个「选择照片」按钮（学期页原「一键导入」按钮删除）。
- 点击「选择照片」弹出**该幼儿专属的相册**（底部弹层），照片一律按时间归类；点选照片（多选、带勾选标记）后点「导入（N）」加入表单的已导入照片行，已导入照片可单张移除；仅导入照片本身，不导入文字。
- 月度评价页：「评价月份」下拉只显示月份、不带年份，选项覆盖本学期全部月份（示例 3—7 月）。相册来源 = 该幼儿的**在园时光**照片，且**只显示所选评价月份当月**的照片（不含往月），当月内按**周次**分组（第 1 周、第 2 周…）。每名幼儿每周有 2 次在园时光照片采集，同一周的两次合并放在该周内，不区分第几次。
- 学期评价页：相册来源 = 该幼儿**过往各月月度评价**中的照片，覆盖**整个学期的所有月份**（3—7 月），按**月份**分组（照片本身存储在月度评价记录下，与数据库结构一致）。

**对 backend 的影响**：
- 在园时光照片需支持按 child_id + month 查询（仅返回该月），月内按周次（自然周）分组；接口无需区分当周第 1/第 2 次采集。
- 月度评价照片需支持按 child_id + 当前学期查询，返回学期内全部月份并按月份分组（即从该幼儿的历史月度评价记录中汇集 file_id）。
- 学期由后端根据当前日期推算（页面月份选项 = 本学期包含的月份，前端不写死年份）。
- 月度评价记录需增加关联照片字段（file_id, 0:k，引用在园时光的文件）；学期评价记录同样需要 file_id (0:k)，引用月度评价中的文件。导入是**引用复制**（复制 file_id 关联），不复制文件本体。

### 5. 综合评估工具替换为《3-6岁儿童学习与发展指南》教师评定量表

**页面**：`screens/comprehensive-assessment-form.html`
**数据源**：`data/guide-scale.json`

**修改内容**：
- 原先每个领域仅 2 条自拟评价语（共 10 题），现替换为 `guide-scale.json` 中的正式量表题库：5 大领域共 **124 题**（健康 36 / 语言 24 / 社会 30 / 科学 23 / 艺术 11）。
- 保持「5 大领域」的组织层级，**不在页面上展开 aspect（维度）与 goal（目标）两层**，题项在所属领域下直接平铺，与改版前的页面结构一致。
- 每道题呈现：题项名称（item_name）+ 提问（question）+ 1/3/5 三级行为锚点（anchors）+ 1—5 分单选。
- 领域卡片改为可折叠（默认收起），避免 124 题一次铺满页面；领域标题右侧实时显示该领域均分。
- 顶部新增「评分说明」卡片，说明 1/3/5 对应三个年龄段典型水平、2/4 为过渡水平。
- 身高体重题（`H1-1-1`，item_type=measurement）标注「实测换算」标签，提示该题由实测数据换算而非教师主观评定；该题下方直接展示身高体重参考范围表（3~4岁 / 4~5岁 / 5~6岁 × 男孩 / 女孩 × 身高cm / 体重kg），数据取自 `guide-scale.json` 的 `reference_table`（来源：2006年WHO儿童生长标准），并附换算说明。
- 默认分值由 4 改为 3（量表中位数）。底部汇总由「五大领域平均分」改为「全部题项平均分」。

**对 backend 的影响**：
- 【题库需入库】综合评估不再是自由文本 + 少量自拟题，而是固定量表。需要将 `guide-scale.json` 的题库结构落库（或作为版本化配置读取）：`domain → aspect → goal → item`，item 字段含 `item_id`（如 `H1-2-3`，逐级截断即得上级 id）、`item_name`、`question`、`anchors{1,3,5}`、`item_type(likert|measurement)`、`anchored_levels`、`inferred_levels`。量表需带 `version` 字段（当前 1.0），历史评估记录须绑定其填写时所用的量表版本。
- 【作答记录粒度】综合评估结果由「每领域一个分数」变为「每题一个 1—5 整数分」：需 `db_comprehensive_assessment_item`（或等价结构）保存 `assessment_id + item_id + score(1..5)`，每次评估 124 条记录。
- 【分数聚合规则】按 `guide-scale.json` 的 `scoring_rules` 实现：goal/aspect/domain/total 分数一律取**其下所有题项得分的均值**（题项级均值，非下级均值的均值）；前端目前只展示 domain 与 total，但 aspect/goal 层级的分数后端仍应可计算并输出，供报表使用。
- 【measurement 题处理】`H1-1-1` 由身高体重实测值对照 WHO 参考表换算得分，不由教师评定；该题可选择性纳入或排除总分，排除时须在报告中标注。后端需保存该题的原始实测值（身高 cm / 体重 kg / 性别）而非仅结果分。
- 【前端不写死题库】页面题目当前由脚本内嵌数据渲染（原型阶段），正式实现应由接口下发题库，前端只负责渲染与提交。

### 6. 综合评估：草稿态落地、个人进度三态统一、返回路径修正

**页面**：`screens/comprehensive-assessment-form.html`、`screens/growth-comprehensive-assessment.html`、`screens/comprehensive-assessment-report.html`、`screens/comprehensive-assessment-class-report.html`

**修改内容**：

6.1 长表单可滚动性
- 124 题的评估表单原先隐藏了滚动条（`.scroll::-webkit-scrollbar{display:none}`），长页面缺少滚动位置提示。现改为显示细滚动条（6px）。

6.2 返回路径修正（信息架构遗留问题）
- 综合评估的三个子页（填写表单、个人评估报告、班级评估报告）的返回键仍指向 `course-building.html`（教研培训部 → 课程建设），但该页早已不含任何综合评估入口（现仅剩一个返回 training-center 的链接）。综合评估的真实入口是：家园共育 → 成长档案 → 教师评价 → 综合评估（`growth-comprehensive-assessment.html`）。
- 三个子页的返回键、报告页的「返回进度」按钮统一改为指向 `growth-comprehensive-assessment.html`；页面 title 由「· 课程建设」改为「· 儿童成长档案」。
- 已核对全部 35 个带返回键的页面，返回目标与实际入口现已全部一致。

6.3 个人进度改为三态 + 草稿可续填
- 状态收敛为三种：**已完成 / 草稿 / 未完成**（原「未开始」改称「未完成」）。判定口径：已评题数 = 124 为已完成；1—123 题为草稿；0 题为未完成。
- 表单默认不再预选 4 分（原先每题默认 4 分会使「完成度」失去意义），改为全部「未评」起始，从而使三态可被真实区分；领域标题显示「已评 x/y · 平均 z」，底部显示「已评 x/124」与当前均分。
- **草稿持久化**：教师每点选一题即自动写入草稿，中途退出页面不丢失；再次进入（`?child=xxx`）自动回填已评分数并可继续填写。原型用 localStorage 模拟，正式实现由后端草稿接口承担。
- 个人进度列表由存储状态动态渲染，操作按钮随状态变化：已完成→查看报告，草稿→继续，未完成→填写；「进度」卡片的完成人数同步由实际状态统计得出。

**对 backend 的影响**：
- 【草稿态是必需的持久化状态，而非前端临时态】综合评估记录需要 `status` 字段，取值三态：`s1=done(已完成)|s2=draft(草稿)|s3=not_started(未完成)`；其中「未完成」等价于无记录，可不落库，由班级花名册左连接得出。
- 【逐题增量保存】需提供「保存单题评分」或「批量保存部分评分」的接口，教师每评一题即可落库，不依赖最终提交；即评估主记录在教师首次评分时就要创建，而非提交时才创建。
- 【状态由题数派生】建议后端存 `rated_count` 与 `total_count`（当前 124，随量表版本变化），`status` 由二者派生而非手工维护，避免与实际作答不一致。
- 【续填读取】需支持按 `child_id`（+ 当前学期/评估周期）读取草稿的全部已评题项，用于表单回填。
- 【完成判定与量表版本绑定】「已完成」= 该量表版本下全部题项均已评分；若量表升版导致题数变化，历史记录仍按其绑定版本的题数判定，不应因升版被重新标记为草稿。
- 【导出口径】导出报告弹层已区分「已完成 / 草稿（导出当前版本）」，后端导出接口需允许导出草稿态的部分数据并在报告中标注未完成。

### 7. 综合评估结果页：只保留雷达图，新增「评估结果 / 明细」双 tab

**页面**：`screens/comprehensive-assessment-report.html`、`screens/assessment-store.js`（新增）

**修改内容**：
- 删除「数据分析」板块（较好/稳定/较弱领域计数 + 逐条文字分析）与「综合评估」板块（整段评语文字）。结果页不再输出任何文字性分析结论。
- 结果展示只保留**五大领域雷达图**及其数据标签（领域名 + 该领域均分）。
- 在姓名卡下方、雷达图上方新增 tab 切换（分段控件），默认选中「评估结果」：
  - **评估结果**：五大领域雷达图。
  - **明细**：按 5 大领域分卡片展示该幼儿全部 124 道题，卡片头部显示该领域均分。每道题为「题项名 + 其下一条 1—5 李克特选项栏」，教师所评的那一档高亮，样式与填写页评分控件一致但为只读（不做右侧单个数字的呈现方式）；未评题项保留完整选项栏、无高亮，并在题项名右侧标注「未评」。
- 雷达图数值不再写死，改为由该幼儿的题项级得分实时聚合出领域均分，与「明细」tab 完全同源，两个 tab 不会出现对不上的情况。
- 姓名卡的状态徽标与副标题随记录状态变化（已完成 / 草稿 x/124 / 未完成），草稿态下未评领域在雷达图与明细中均显示「未评」且不计入均分。
- 【重构】新增 `screens/assessment-store.js`，把量表结构（5 领域 × 124 题的 id/名称）、幼儿名单、草稿存储与状态判定收敛为单一数据源，由填写页 / 进度页 / 结果页共同引用，避免三处各自维护一份题库与状态口径。

**对 backend 的影响**：
- 【报告接口精简】综合评估报告接口不再需要返回文字分析字段（强弱项描述、综合评语）。若 spec 中已有此类字段（如 `analysis_text` / `assessment_summary`），可从教师端结果页的返回体中移除；如成长册等其他场景仍需评语，应由对应场景单独提供，不再由本接口承担。
- 【报告需返回题项级数据】结果页的「明细」tab 需要逐题得分，因此报告接口需返回题项级明细（`item_id + score`），而不只是 5 个领域均分；领域均分应由题项分聚合得出（与第 5 条的 `scoring_rules` 一致），避免前后端各算一次导致口径不一致。
- 【未评语义】草稿态记录中未评题项需明确返回「无分数」而非 0，领域均分只按已评题项计算；聚合接口需能返回「该领域尚无任何评分」的状态。

### 8. 班级评估报告页：同样只保留雷达图

**页面**：`screens/comprehensive-assessment-class-report.html`

**修改内容**：
- 与第 7 条个人报告页保持一致：删除「强弱项分析」板块（优势/稳定/支持领域计数 + 逐条文字分析）与「班级综合评估」板块（整段班级评语文字）。页面不再输出任何文字性分析结论。
- 只保留**五大领域雷达图**及其数据标签（领域名 + 班级该领域均分），底部保留「继续评估 / 返回进度」导航按钮。
- 顶部概览卡的「已完成」「班级均分」不再写死（原为 `10/30`、`4.1`），改为按实际评估记录实时统计；说明文案同步显示实际份数。
- 汇总口径：班级报告**只统计「已完成」的评估**，草稿不计入（与「基于已提交的 N 份」的文案口径一致）；领域均分按题项级汇总（取全部已提交幼儿在该领域的所有题项得分求均值），而非各幼儿领域均分再平均，避免题项数不等造成加权失真。
- 空态处理：当班级尚无已完成评估时，均分显示「—」，雷达图不绘制数据点，各领域标注「未评」，并提示「暂无已完成的评估」。

**对 backend 的影响**：
- 【班级报告接口精简】同个人报告，班级报告接口不再需要返回强弱项文字分析与班级评语字段。
- 【班级聚合口径】班级领域均分需在后端按题项级聚合（∑该领域所有已提交幼儿的题项得分 / 题项数），与 `guide-scale.json` 的 `scoring_rules`「题项级均值」一致；不可用「各幼儿领域均分的平均」代替。
- 【统计范围】班级汇总只纳入 `status=done` 的评估记录；接口需返回已完成份数与班级总人数，用于「已完成 x/y」展示。
- 【空态】班级无已完成评估时，接口应返回明确的空结果（而非 0 分），供前端区分「均分为 0」与「尚无数据」。

### 9. 成长册：内容可拖动排序、新增样本预览与批量导出、检查表横向滚动

**页面**：`screens/growth-book.html`、`screens/growth-book-sample.html`（新增）

**修改内容**：

9.1 成长册内容维持「仅勾选」
- 曾尝试改为「勾选 + 拖动排序」（可决定各内容在册中的位置），但纵向排序列表在页面上过于笨重，**2026-08-01 评审决定回退**：保持原 2 列勾选网格，只决定是否收录，不提供顺序调整。
- 成长册内容顺序仍为第 2.5 条确定的固定顺序：园所介绍 → 在园时光 → 亲子活动 → 期末评估 → 综合评估 → 教师寄语。

9.2 新增两个功能入口
- **成长册样本**（`growth-book-sample.html`，新增）：只展示**一份版式样本**（示例幼儿），不引入本班幼儿数据；页序 = 封面 → 各勾选栏目 → 封底；支持点击书页或左右按钮翻页，翻页为绕左边缘的 3D 翻转动画；提供「导出 PDF」。
- **导出报告**：底部弹层批量导出，与综合评估的导出交互一致；不可生成的幼儿**仅置灰并禁用勾选**，不展示缺失项说明（避免后端为每名幼儿逐项计算缺什么）。

9.3 生成检查表
- 栏目较多时表格可横向滚动（原共用样式把横向滚动条隐藏了，此页覆盖为可见细滚动条）。
- 「幼儿」列横向滚动时固定在左侧（sticky），避免滑动后认不出是谁。
- 表格最小宽度按实际列数计算，列少时不产生多余滚动。
- 末列由「结果」改为「**查看**」：内容齐备的幼儿显示可点击的「查看」，进入该幼儿的成长册翻页页面（`growth-book-view.html?child=xxx`，新增）；不可生成的幼儿该处置灰不可点。即「能否生成」由该列的可点击状态表达，不再单独用一个结果 dot。

9.4 状态判定改为真实计算（原为写死数值）
- 「可否生成」= 该幼儿**所有已勾选栏目**是否齐备；取消勾选某栏目后，仅缺该栏目的幼儿会随之变为可生成。
- 「班级生成进度」= 可生成人数 / 班级人数（原为 `58 + 勾选数 × 2.6` 的假公式，与实际数据无关）。

9.5 代码组织
- 书本渲染与翻页逻辑抽为 `growth-book-render.js` + `growth-book-viewer.css`，由样本页与单个幼儿查看页共用，避免两套翻页实现。

### 10. 成长册「编辑样板」与「成长资料」（2026-08-01 逐项确认）

**页面**：`screens/growth-book-edit.html`（新增）、`screens/growth-book.html`、`screens/growth-book-sample.html`、`screens/home-school-moment-feed.html`、`growth-book-render.js`

**修改内容**：

10.1 编辑样板（模版级配置集中地）
- 样本页的「导出 PDF」替换为「编辑样板」；成长册主页的「成长册内容」勾选卡片改为一行摘要 + 编辑入口，模版相关配置全部收敛到编辑样板页（避免配置散在两处）。
- 页面顶部是缩放后的书本实时预览，改动即时可见。
- **封面**：从 4 套预设版式中选一套（满版图+底部文字 / 上图下文 / 左文右图 / 纯色居中）+ 更换封面图片 + 修改标题文字。不提供自由拖拽定位（手机端难操作、易排丑、后端需存坐标）。
- **预设栏目勾选**：6 项，从主页迁入。
- 修改即时生效，无草稿态；页面底部为「完成」而非「保存」。

10.2 新增栏目（临时起意的材料）
- 教师可新增栏目（如「入学第一天」「最后一天」），填写：栏目名称、**栏目说明文字（全班共用，由教师撰写）**、插入位置。
- **插入位置**：下拉选择「插在某栏目之后」（含「封面之后」）。预设 6 项仍为固定顺序，只有新增栏目需要定位——以此保留位置控制而不恢复整体拖拽排序（见 9.1）。
- **内容来源**：由**家长上传**。新增栏目时自动向家长发起「成长册素材征集」，与亲子活动**分开**（若复用亲子任务，按既有规则会落进册子的亲子活动栏目，位置错误）。
- **缺交兜底**：新栏目纳入齐备判定；教师可在「提交情况」中替未交的幼儿代传，避免个别家长拖住整班出册。

10.3 成长资料（在园时光 → 成长册的通道）
- 在园时光的每条动态新增加入按钮；点击弹出该动态的全部照片，教师**勾选要收录的若干张**（如 10 张挑 3 张），**活动文字（标题+描述）全量收录**。
- 按钮位于动态卡片**左列、头像正下方**（26px 圆形图标按钮），标题行右侧保留发布时间；未收录显示描边「+」，已收录显示实心「✓」，含义由 title/aria-label 提供。卡片底部同时显示「已加入成长资料 · 收录 N 张照片」。
- 成长资料为**班级级素材**：收录后全班每本册子的「在园时光」栏目内容相同。
- 已收录内容在编辑样板的「在园时光」栏目中管理（可移除、可调整先后）。
- 亲子活动不需要此机制——所有亲子活动一律进册子。

10.4 生成检查表结构调整
- 「园所介绍」「在园时光」为园所级/班级级内容，全班相同，原先在幼儿列里 5 个点永远一致，属零信息量的列。现移出表格，改为表格上方的**班级级前置条件**（显示就绪状态与成长资料条数）。
- 表格只保留因人而异的栏目：亲子活动、期末评估、综合评估、教师寄语 + 各新增栏目（家长征集），列数从 6 降到 4+N。

10.5 三项边界确认（2026-08-01）
- **新增栏目数量**：不设上限。实际使用中不会加太多，暂无限制的必要；前端与后端均不做数量校验。
- **素材征集的时限**：**不设截止日期**，以学期结束为自然边界。催办走**线下**（教师当面/群里提醒），系统不做倒计时、不做到期提醒、不做逾期状态。
- **模版与生成的时序**：模版**定好之后才生成**册子，不会在生成过程中或生成后修改。因此不需要模版版本管理、不需要为已生成的册子保留模版快照、不需要「模版变更后重新生成」的流程。

**对 backend 的影响**：
- 【模版对象扩展】`db_growth_book_template` 需扩展：`cover(layout_id, image_file_id, title_text)`、`custom_sections[]`。仍**不需要**预设栏目的 sort_order（预设顺序固定）；但新增栏目需要 `anchor_after`（值为 `cover` 或某 component_key）来确定落位。
- 【无需版本与快照】模版定好再生成（见 10.5），因此 `db_growth_book_template` **不需要 version 字段**，已生成的册子也**不需要绑定模版快照**；模版直接可改，不做变更历史。注：此处与第 5 条综合评估量表的处理**不同**——量表必须版本化（历史评估要按当时的量表口径判定），成长册模版不需要。
- 【征集无时限字段】`db_book_material_request` **不需要 due_date / 逾期状态 / 提醒任务**，催办在线下完成；只需记录发起时间与提交情况。
- 【不做数量校验】新增栏目数量不设上限，接口无需限制 `custom_sections` 的长度。
- 【新增栏目对象】`db_growth_book_section`：`section_id, template_id, name, note_text(全班共用), anchor_after, created_by, created_at`。
- 【新增一类家长提交】「成长册素材征集」独立于 `db_parent_task`：`db_book_material_request`（关联 section_id）+ `db_book_material_submission`（`child_id, file_id(1:k), submitted_by(parent|teacher), submitted_at`）。`submitted_by` 需区分家长提交与**教师代传**，用于统计与追责。
- 【成长资料】`db_growth_material`：`material_id, class_id, moment_id(来源动态), title, description(全量), file_id(1:k, 教师挑选的照片子集), sort_order, created_at`。班级级，不按 child_id 存；册子生成时全班共用同一批。
- 【可生成判定升级】齐备判定 = 班级级栏目就绪（在园时光要求成长资料非空）+ 该幼儿的因人而异栏目齐备 + **每个新增栏目均已有该幼儿的提交**。接口仍只返回布尔 `can_generate`（见第 9 条，不返回缺失明细）。
- 【班级级 vs 幼儿级】接口需按此二分返回：班级级栏目返回一个班级状态，幼儿级栏目才按 child_id 返回；不要再为班级级栏目逐个幼儿返回相同的状态值。
- 【封面图片】封面图为模版级素材，需 file_id 管理；原型用本地 FileReader 预览，正式版走园所素材库上传。

**对 backend 的影响**：
- 【成长册模版配置需持久化】成长册「收录哪些内容」是园所/班级级配置，需落库（建议 `db_growth_book_template`：`template_id, school_id, class_id, items[](component_key, enabled), updated_at`）。**不需要 sort_order 字段**——顺序固定，见 9.1。
- 【组件枚举】component_key 枚举与第 2.5 条一致：`intro(园所介绍)|time(在园时光)|task(亲子活动)|term(期末评估)|comp(综合评估)|message(教师寄语)`。
- 【可生成判定依赖模版】「是否可生成」不再是固定 6 项齐备，而是**按当前模版已启用的项**判定；接口只需按幼儿返回一个布尔值 `can_generate`，**不需要返回缺失项明细**（前端不再展示缺什么，以减少后端逐项计算）。
- 【单个幼儿成长册查看】需支持按 `child_id` 返回该幼儿成长册的分页内容，用于「查看」列打开的翻页预览；预览内容须与最终 PDF 同源，避免看到的与导出的不一致。
- 【批量导出】需要批量导出任务接口：入参为幼儿 id 列表 + 模版 id，返回任务 id；导出为异步任务（逐个渲染 PDF 较慢），需要任务状态查询与结果下载。
- 【PDF 渲染】样本预览与最终 PDF 必须同源渲染，避免预览与导出不一致；建议服务端按「背景图 + 占位框 JSON」渲染，详见下方模版材料清单。
- 【模版素材】需要新增素材管理：背景图（封面/封底/内页/各栏目章节页）、园所 LOGO、字体文件及授权，均需 file_id 管理并与模版绑定。

---

## 2026-08-02 · 后端答复（闭环记录）

> **方向与上文相反。** 第 1—10 条是前端改完写给后端看的；本节是**后端已经给出的答复**，其中包含它**推翻或更正**本仓库假设的地方。
>
> 权威来源：`../hualong-backend/DECISIONS.md` E 组（E1—E7，以及 E3 之下的 W1—W21）与 `../hualong-backend/db/GAPS.md` G27—G31。本节只做转录与对照，**不在此重新定义规格**；若发现与后端不一致，改的是本仓库的原型，不是后端的结论。
>
> 后端决议日期为 2026-08-01，表数链条：B 组把 45 张表压到 37，E 组再扩回 45（拔掉的是瘦关联表与重复实体，加回的是教师寄语、逐题量表、成长册 widget 版式）。

### 11. §1 社区共育：两个后端方案**都不采用**，前端改动本身完全正确

**对应**：本文件第 1 条（2026-07-31）。

**后端结论**（`DECISIONS.md` E5，依据 B11）：

- 第 1 条提的两个方案 —— a) 给 `db_community_submission` 加 `parent_task_id` 外键、b) 给它加 `task_type` 字段 —— **两个都不采用**。B11 已判定 `db_community_submission` **整张表拔除**（表数 38 → 37）：逐栏比对后它与 `db_parent_task` + `db_parent_task_submission` 完全重叠，且它连 `teacher_id` 都没有，正是「本该从任务派生」的痕迹。上游术语表把「社区教育」定义为 a type of parent-child task，`APP-STRUCTURE.md` 06 也只有 `TaskType → 普通亲子任务 / 社区教育任务` 这一条路径，独立的社区展示实体在上游规格里没有依据。
- 社区共育是 **feed 视图**，不是实体。筛选直接用**早已存在**的 `db_parent_task.parent_task_type`（`t1=daily | t2=community`），不需要任何新字段。
- **前端这条改动是对的**：筛选器由「任务名称」改为「任务类型」正是 B11 要求的 UI 修正，前端自行做到了。新增的 `data-type="daily|community"` 即对应 `parent_task_type`；`data-task`（具体任务名）不再参与筛选，保留无妨。
- feed 由任务自身的 `publish_status` 驱动，**不需要精选旗标**。

**对本仓库的影响**：`community-coeducation.html` 无需再改。后端侧 `ui=community.card.task_label` 会从 `db_community_submission.task_label` 改指 `db_parent_task.parent_task_type`（作筛选用），9 笔 `ui=community.*` 重新对映 —— 这发生在 `05 home-school-spec.md`，不在本仓库。

### 12. §5 更正：`guide-scale.json` 的 `version` 与 `scoring_rules` **不在顶层**

**对应**：本文件第 5 条。

第 5 条写「量表需带 `version` 字段（当前 1.0）」与「按 `guide-scale.json` 的 `scoring_rules` 实现」，读起来像这两个键在顶层。**实际不是。** `data/guide-scale.json` 顶层只有两个键：

```
guide-scale.json
├── instrument          ← name / version("1.0") / source_document / extraction_scope
│                          rater / response_format / level_semantics / hierarchy
│                          id_scheme / scoring_rules / counts
└── domains             ← 5 领域 → aspect → goal → item
```

`version`、`scoring_rules`、`response_format`、`level_semantics`、`hierarchy`、`id_scheme`、`counts` **全部在 `instrument` 之下**。后端 E2 引用时写的是 `instrument.scoring_rules`。

**同时更正一处路径**：第 5 条写身高体重参考范围表「取自 `guide-scale.json` 的 `reference_table`」，它既不在顶层也不在 `instrument`，而是挂在 `H1-1-1` **题项自己身上** —— `domains[0].aspects[0].goals[0].items[0].reference_table`，同一处还有 `measurement_note`。

**对本仓库的影响**：任何读取脚本的路径必须是 `guide-scale.instrument.version` / `guide-scale.instrument.scoring_rules`，不是 `guide-scale.version`。题数亦不必另行统计，`instrument.counts` 已给出 `{domains:5, aspects:11, goals:32, items:124, likert_items:123, measurement_items:1}`。第 5 条原文按此更正理解。

### 13. §5 `H1-1-1` 身高体重：后端定为**教师照参考表主观评定**，且这是对量表原典的一处**明示偏离**

**对应**：本文件第 5 条「measurement 题处理」。

第 5 条按量表原文写「由实测数据换算，不由教师评定」，并要求后端保存原始实测值（身高 cm / 体重 kg / 性别）。**后端不这么做**（`DECISIONS.md` E2、`GAPS.md` G27）。改为**教师看着参考表自行给 1—5 分**，参考表降级为判断辅助。三条理由叠在一起：

1. **没有采集点。** 三个前端共 71 个页面没有任何一处收身高体重，schema 也没有落点。
2. **就算收了也算不出分数 —— 评分规则本身欠缺定义。** 参考范围大幅重叠：男孩 3~4 岁身高 94.9-111.7、4~5 岁 100.7-119.2、5~6 岁 106.1-125.8，**一个 108cm 的男孩同时落在三段**，得 1 分、3 分还是 5 分，量表没说；体重同理（20kg 也落在三段）。而且**身高与体重可能落在不同段**，原文「身高、体重达到 X 岁参考范围」没说要两者同时满足还是取其一。要实作就得自己发明一套消歧规则 —— 那套规则是我们的，不是《指南》的。
3. **合规成本。** 身高体重属 PIPL 第二十八条明列的**医疗健康敏感个人信息**，B12 正是为此砍掉成长册的体检数据。

**正面副作用**：完全不采集身高体重，就没有医疗健康敏感资料进入系统，G27 的合规那一半自动消失，不需另立同意用途、留存规则与告知文本。

**必须记录在案的代价**：我们的实作**不是《指南》原典的忠实实作**。`H1-1-1` 在我们这里实质上是一道 likert 题。题库入库时 `item_type` 仍存 `measurement`（忠于原始 JSON），但后端会在 `db/rubric/` 的说明文件里写明这处偏离，避免日后有人拿我们的数据与《指南》对照时误以为是实测换算。`required_count` 维持 **124**，`db_child.birth_date` 与 `gender` **不参与此题计分**。

**对本仓库的影响**：`comprehensive-assessment-form.html` 上 `H1-1-1` 的「实测换算」标签与「该题由实测数据换算而非教师主观评定」的提示语**与本决定不符，需改**；参考范围表保留（它现在的身份是教师的判断辅助），换算说明改写为主观评定的说明。

### 14. §2.5／§3「一律二元」与 §6.3「三态」的冲突：E4 已定死口径

**对应**：本文件第 2.5 条、第 3 条（所有进度表一律二元）与第 6.3 条（综合评估三态 + 草稿落库）。这两处同时成立只有一种读法，后端 E4 明确定死，避免后端两处返回不同值：

| 位置 | 状态数 | 口径 |
|---|---|---|
| 综合评估自己的进度页 `growth-comprehensive-assessment.html` | **三态** | 已评 = 124 → 已完成；1—123 → 草稿；0 → 未完成（等价于无记录，可不落库，由花名册左连接得出） |
| 月度评价／学期评价自己的页面 | 二元对外 | 草稿态仍在 `db_month_eval.month_eval_status` 内部保留（e1/e2/e3），只是不对外显示 |
| 教师评价聚合页、成长档案首页、成长册检查表 | **二元** | **草稿一律折算为未完成** |

即：**草稿只在综合评估自己那一页可见，任何聚合视图都把草稿并入「未完成」。**

其余连带（E4）：`status` 由题数派生、不手工维护 —— 存 `rated_count` 与 `total_count`（总数随量表版本变）；导出报告允许导出草稿态的部分数据并在报告中标注未完成。

### 15. §10.2 的「栏目说明文字」（`note_text`）取消

**对应**：本文件第 10.2 条与第 10 条的【新增栏目对象】。

后端 W11 砍掉 `db_growth_book_section.note_text`：**有了 widget 之后它是多余的** —— 教师在该栏目页上放一个 `literal` 文字 widget 打同一段话即可，而且还能自选位置与大小，比一个固定位置的说明栏更强。

同批砍掉的还有 `class.intro`（B12 加的 `db_class.class_intro`），无使用场景，**B12 该项作废**；`db_school.school_intro` 保留。

**对本仓库的影响**：`growth-book-edit.html` 新增栏目表单里的「栏目说明文字」输入框需去掉，改为在该栏目的网格里放一个文字 widget。新增栏目的字段收敛为：栏目名称 + 插入位置（`anchor_after`）+ 该栏目的 widget 版面。

### 16. 成长册版式契约（E3 + W1—W21）—— 本仓库最大的一块，已定的部分不得再自行发挥

第 9、10 条提出的「模版」被后端接下并大幅具体化。E3 **推翻 B12 的「模板即导出页、v1 不需要 `template_code`」** —— 模版现在是实打实的版式配置对象。以下为已定条款，编号对应 `DECISIONS.md` 的 W 系列：

**版面几何**
- **W1** 版面单位是**实体 A4 页**（210 × 297mm 直式），不是连续画布。**widget 不得跨页**，每个 widget 完整归属于单一页面，因此不存在切断、下推、留空洞这类分页副作用。**W2** 一个栏目可含多页，页数由教师新增。
- **W1a** 网格 **15 × 24**，格子 **10mm 精确正方，不接受近似**。左右边距 30mm → 内容宽 150mm ÷ 15 = 10.0mm；上下边距 28.5mm → 内容高 240mm ÷ 24 = 10.0mm，余数 0。上下取 28.5 而非 30 的理由：`150 : 237` 约分为 `50 : 79`，79 是质数，四边都恰好 30mm 的唯一整除解是 50 × 79（格 3mm，过细）。
  **精确正方是产品要求不是美观要求**：教师拉一个 2 × 2 的图片 widget 就是在宣告「这里要一张 1:1 的照片」，家长端裁剪框直接吃这个比值。实作上先算格边长、两轴共用同一个整数像素值（`cell = floor(content_width_px / 15)`，`rows = floor(content_height_px / cell)`，余数并入下边距），不要分别从宽高去除。
- **W1b** 边距是**先扣掉的、有功能的区域**，用来放**美术边框**。边框是范本级素材，必须跟着进 PDF；**widget 不得放进边距**（`grid_x ∈ 0..14`、`grid_y ∈ 0..23`）。内容区占整页 58%，**边框素材是这个决定的前提，不是选配**。边框归我们设计、不提供上传（W19）。
- **W1c** **取消「手机端不允许横向滑动」，改为允许 zoom。** fit-width 时每格仅 18.6pt，远低于 iOS 建议的 44pt，需放大 2.37 倍才达标。连带三条互动规格（前端待定但必须有答案）：平移手势与拖曳 widget 的手势必须能区分、拖到视窗外时需边缘自动卷动、任何触控舒适倍率下画面永远只看得到约 9 格。
- **widget 最小 2 × 2 格**（四角缩放把手的热区否则重叠），后端存档校验 `grid_w >= 2 AND grid_h >= 2`。

**编辑权限**
- **W13** **预设页面全部由我们（后端／开发方）作者，存为仓库里的版本化 JSON，教师不能改。** 这修正了 W3「网格适用全册」的措辞 —— 全册都是网格版面，但预设 6 个栏目的页面预先排好、开箱即用。放 repo 不放 DB 的理由：`CLAUDE.md` 红线 5 订明生产环境业务表初始为空，版式库进 DB 就成了一类「必须存在于生产的产品参考资料」，那个类别不存在。
  **教师能配置的只有三件事：开关栏目、选封面、新增栏目**（新增栏目仍可自由排版）。放弃「可改但不建议改」的理由：override 表、「还原预设」语意、班班版面分歧、版式库更新谁跟得到谁跟不到 —— 一整层代价换来一个我们自己都不希望教师使用的功能。
- **W5** 一个 widget 一列，不存 JSON 数组（**刻意不套用后端既有的「瘦关联表改 JSON」偏好**）：widget 有真实生命周期，且素材提交必须外键指向它，存 JSON 则 `widget_id` 是数组元素、无法被外键指向。
- **W6** **重叠一律拒绝放置，不做弹开推挤**（手机小屏上连锁位移教师看不懂）。放手时重叠则标红 + 提示移走 + 关闭存档按钮；坐标重叠时拒绝存档整个栏目。**服务端须重做一次校验** —— 前端 UI 永远不是完整性边界。
- **W7／W8** widget **只有图片、文字两型，不做视频**；不为同功能不同尺寸各开模板，一律可自由缩放。文字 widget 基本控制项：字级、对齐；进阶控制放抽屉。
- **W9／W10** 图片比例由格子坐标反推；家长上传时**不强制**符合该比例，显示方式（Fill / Cover / 自动裁切）由**教师**在网格端决定；家长端附裁剪工具，上传时即预览最终显示比例。

**内容绑定**
- **W11** **只用一栏 `binding_key`，不开第二栏 `content_source`**；粒度、谁填、是否入齐备判定、能配哪种型别，全是绑定目标的固有属性，查程序里的登记表即可，不在每列复写。登记表：`literal`（唯一内容真存在 widget 列上的一种）、`collected`（家长上传，**也吃文字**）、`school.intro`、`class.material`、`child.message`、`child.term_eval`、`child.task`、`child.assessment`。`collected` 的文字是 UGC，与照片走**同一条内容把关**。
- **W12** **雷达图不存任何档案、不存 base64、不落任何字段，两端即时算。** 五领域均分完全由题项分推导（题项级均值），小程序端 canvas 直接画、PDF 端服务器绘向量。这条适用于**所有** bound widget：widget 只记「这个盒子要显示什么」与「盒子多大」。
- **W18** **文字上限由框的大小推导，结构上不可能溢出。** 不定全域字数上限，前端依 `grid_w × grid_h` 与当前字级即时算 `maxlength`，打满就打不下去。反方向同时成立：bound 型 widget 的**框必须大于等于来源的字数上限**，编辑器挡掉太小的框。边角：教师打完字再调大字级**必须挡住存档并提示，不可默默截断**。

**不定长内容**
- **W14** 用「**重复页样板池 + 带种子的伪随机挑选**」：我们为该栏目设计 3—4 个页版式（各自宣告容量），渲染时挑版式、重复铺，直到盖完实际件数；挑选带随机性避免整本每页长一样。**随机必须可重现** —— 否则违反第 9 条「预览与 PDF 同源」。做法：`db_growth_book` 增 `layout_seed`（首次渲染时产生，之后永不变），挑选序列 = `PRNG(layout_seed, section_key, page_index)`。同一本册子重复渲染多少次都是同一个版面，不同幼儿因 seed 不同而版面不同。

**征集与状态机**
- **W15** 素材征集**以栏目为单位发起**（不按 widget、不按页）：一个新增栏目 = 家长端一则待办，点进去列出该栏目所有页上的全部 `collected` 槽位，一次交完。**齐备判定 = 该栏目全部槽位都有东西，不是「至少交一件」。** 不接受「可以不交」的理由：允许缺件等于把压力与责任转嫁给教师（设计版面时就得预判哪格家长交不出来），且缺件必然产生留白、违反教师设计原意。缺件补救只走教师代传。
  已知代价：教师多摆两个槽位就可能卡住全班出册 —— 与「新增栏目不设上限」「征集不设时限」叠加，是三条已定决策共同造成的风险，不另设机制。
- **W16** **成长册有草稿匣，定稿才发布；定稿后模版冻结。** `template_status`：`d1=draft`（可编、可预览）／`d2=published`（冻结）。发布之前是草稿，发布之后**只能撤回**；**撤回的语意是删除不是保留** —— 该栏目已收的提交一并删掉，重新发布时家长要重交（比例已失效且无原图可重算，也符合 PIPL 第六条）。系统不提供「发布了再偷偷改一下」的中间态；**教师在发布前有责任先预览**。
  这把第 10.5 条的「模版定好之后才生成」从**使用假设**变成**系统约束**，因此「不做版本化、不做快照」的结论不变，但现在有东西保证它。

**存储与渲染**
- **W17** 家长上传的照片：上传当下统一转 **JPEG（MozJPEG 编码，q82—85）、长边 2000px**，**只存裁切后的成品，不存原图**。选 JPEG **与压缩率无关** —— PDF 原生只支援 `DCTDecode`（JPEG）与 `JPXDecode`（JPEG 2000），**WebP / HEIC / AVIF / JPEG XL 一个都进不了**；存它们等于每次生成 PDF 都要解码重编，批量导出是上千次转码跑在 2 核 VPS 上。输入端照单全收（HEIC / WebP / PNG 都接），服务器统一转，转码只在上传当下发生一次。长边 2000px 的依据：300 DPI 满版一张图只需 1772px。
- **W20** **一份 canvas 绘图代码跑两个环境**，精度要求像素级一致度 ≥ 95%。小程序端 canvas 画 → 逐页出 JPEG → 自组极简 PDF → `wx.shareFileMessage`；服务器端 node-canvas 跑同一份代码，**只在批量导出时**启动。**单本与预览在客户端，服务器零渲染成本。** PDF 走**光栅 150 DPI**（A4 = 1240 × 1754），因为**PDF 的用途是传阅给没装小程序的亲友，不是拿去印刷店印实体册**。唯一的漂移来源是字体，两端必须载同一个字体档。
- **W21** **页数只设软上限，超过只提示不阻挡**（硬上限与「新增栏目不设上限」矛盾，且教师往往到最后一刻才撞墙）。先撞墙的是**档案大小不是渲染时间**：每页约 400KB，40 页 ≈ 16MB、167 页 ≈ 67MB。提示落在生成检查表与批量导出弹层，显示预估页数与档案大小。

**归属**
- **W19** **封面归园所**（`db_school.book_cover`，只有 admin 能改，同一所幼儿园发出去的纪念册封面应该长一样）；**栏目开关与新增栏目归班级**（教师自己班的事，「入学第一天」「毕业典礼」本来就逐班不同）。

**本仓库第 9、10 条被后端确认或改写的点**
- 第 9.1「回退拖动排序、预设 6 项顺序固定」—— **采纳**，故模版不需要 `sort_order`。
- 第 9.2「导出报告改底部弹层」—— **采纳**，并**推翻 B12 曾提的独立页面 `export-growth-book.html`**（该页从未在本仓库建立，后端 `screens.tsv` 里的重复登记见 `GAPS.md` G30）。导出为异步任务：任务 id + 状态查询 + 结果下载。
- 第 9 条「`can_generate` 只回布尔、不回缺失明细」—— **采纳**。
- 第 10.4「班级级栏目移出幼儿表格」—— **采纳**，接口按班级级／幼儿级二分返回。
- 第 10 条【模版素材】里的「字体文件及授权」—— **从选配变成硬需求**（W20），且授权必须涵盖服务器端嵌入与再散布。
- 第 10 条建议的 `db_book_material_request` **不建表** —— 一个 parent-source widget 的存在本身就是一次征集，状态由提交记录的有无派生。

### 17. 六个新页面需先在后端 spec 补 `ui=` 标注，本仓库才能接上 harness

本批新增的六个页面尚未进入后端的绑定契约链条：

```
teacher-evaluation.html · teacher-message.html · teacher-message-detail.html
growth-book-edit.html   · growth-book-sample.html · growth-book-view.html
```

后端的契约链条是 `backend spec 的 ui= 标注（权威）→ ui-binding.tsv（生成物）→ 前端 data-ui 属性`。因此顺序是：**先在 `hualong-teacher/docs/backend spec files/05 home-school-spec.md` 补上这些页面写入控件的 `ui=` 标注**，再由后端重跑抽取，前端才能在控件上挂对应的 `data-ui`。

**该文件由另一个 agent 处理，本记录只登记依赖，不在此改动它。** 两个共用脚本 `assessment-store.js`、`growth-book-render.js` 不是页面，不进 `screens.tsv`。另注：目前三个前端仓库**零个控件带 `data-ui`**，补齐是前后端对接的第一项工作；前端可自由更换文案、样式、图片、位置，但 `data-ui` 属性必须原样保留。

### 18. 仍然开放，**不得自行假设**的项目

以下在后端仍标记为未定。原型里若必须先画点东西，请标为占位并在此登记，不要把占位当成结论：

- **末页不满怎么配**（`GAPS.md` G29 (a)）—— 剩 2 件而最小版式容量是 3，是要求版式库备齐 1/2/3/… 各种容量，还是允许留白？**未定。**
- **字体选型与授权**（`GAPS.md` G29 (b)）—— 两端必须载同一字体档才守得住 95% 精度，CJK 字体 5—15MB 需子集化，授权须涵盖服务器端嵌入与再散布。**未定。**
- **`db_term_eval.eval_text` 字数上限**（`GAPS.md` G31）—— 月评是 500，学期评未定。**这一项卡住版式库**：W18 规定 bound 型 widget 的框必须大于等于来源的字数上限，不定就无法决定「期末评估」那一栏的框要多大。

另有两项标记为 **[待查证]**，查证前不当结论用：

- 微信 `chooseMedia` 在 iOS 上回的是 HEIC 还是已由微信转成 JPEG，以及 WebP 在两端 `image` 组件的显示支援 —— 只影响上传端要不要自己转，不影响存储格式的结论（仍是 JPEG）。
- `wx.shareFileMessage` 的档案大小上限 —— W21 的提示门槛按它定，暂以 **40 页 / 16MB** 占位。

以及一项已登记但未收敛的量：在园时光素材数量上限（`GAPS.md` G29 第 3 项）—— 一千张照片每页 6 张就是 167 页，靠成长资料挑选环节自然收敛还是设硬上限，**未定**。
