.hl-card + .hl-card {
  margin-top: var(--space-2);
}

.activity__meta {
  display: flex;
  gap: var(--space-2);
}

/* 时间是这张列表存在的理由（票据 12），所以它比地点重一档。 */
.activity__time {
  color: var(--accent-dark);
}
