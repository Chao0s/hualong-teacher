<!-- 教研培训 —— 对照原型 screens/training-list.html。
     原型把「最新研修」和「历史研修」写成两段结构相同的 HTML，这里合成一份数据两次循环。 -->
<view class="page">

  <view class="sec"><text class="sec__title">我的档案</text></view>
  <view class="profile-grid">
    <view
      class="profile-card"
      hover-class="card--hover"
      hover-stay-time="80"
      wx:for="{{profiles}}"
      wx:key="key"
      data-key="{{item.key}}"
      bindtap="onProfileTap"
    >
      <view class="profile-icon profile-icon--{{item.key}}"></view>
      <text class="profile-title">{{item.title}}</text>
      <text class="profile-desc">{{item.desc}}</text>
    </view>
  </view>

  <view class="hint" wx:if="{{loading}}">正在加载研修…</view>
  <view class="hint" wx:elif="{{error}}" bindtap="onRetry">{{error}}　点此重试</view>

  <!-- wx:for 与 wx:else 不能挂同一个节点（npm test 第 6 项），所以外面套一层 block。 -->
  <block wx:else>
  <block wx:for="{{groups}}" wx:key="key" wx:for-item="group">
    <view class="sec">
      <text class="sec__title">{{group.title}}</text>
    </view>
    <view
      class="card"
      hover-class="card--hover"
      hover-stay-time="80"
      wx:for="{{group.items}}"
      wx:key="id"
      data-id="{{item.id}}"
      bindtap="onTrainingTap"
    >
      <view class="card-top">
        <text class="card-title">{{item.title}}</text>
        <text class="badge">{{item.badge}}</text>
      </view>
      <text class="card-meta">{{item.meta}}</text>
      <text class="card-summary">{{item.summary}}</text>
      <view class="card-foot">
        <text class="pill" wx:for="{{item.pills}}" wx:key="*this" wx:for-item="pill">{{pill}}</text>
      </view>
    </view>
    <view class="hint" wx:if="{{!group.items.length}}">这一区暂时没有研修。</view>
  </block>
  </block>

</view>
