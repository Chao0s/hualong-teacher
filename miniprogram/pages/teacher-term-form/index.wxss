@import '/styles/home-school-common.wxss';
@import '/styles/album-sheet.wxss';

/* 填写学期评价 —— 页面本身没有额外样式，全部来自上面两份共用样式。 */

.page {
  padding: 24rpx 32rpx 56rpx;
}

.form-label--first {
  margin-top: 0;
}

/* 置灰：照片导入没有数据源，入口留着但不可用。提交过的学期评价同样置灰保存按钮。 */
.btn--off {
  opacity: .45;
}
