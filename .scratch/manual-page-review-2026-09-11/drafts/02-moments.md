# 02: 在园时光发布、照片与幼儿关联

**What to build（本票交付）：** 教师确认内容和涉及幼儿后发布活动，重新进入仍看到正确内容与周进度。

**Blocked by：** None（可立即开始页面审核；写入、回读需 README 中的测试环境与样本就绪）。

**Status：** reviewing（原范围已通过；补充G“全部活动移出成长册”已修，待界面复查）

**审核范围：** 仅教师端。家长账号操作和真实收发由外部负责，不作为本票通过前置；教师端已有的家长状态、回复和投稿仍需检查，缺样本交给助手准备或登记。

**拟确认后的执行状态：** ready-for-human-review

**优先级：** P0；P0 优先核对家园沟通，P1 紧随其后，P2 后续逐页扫查。

## 从哪里进入

底部「家园社共育」→「在园时光」→「发布活动」；发布后返回「在园时光」→ 右侧「全部活动」。

## 准备什么

两名测试幼儿、容易区分的横图与竖图各一张；另准备超过三张的活动用于检查大图入口。使用测试班级。

## 按顺序操作

1. 填写「活动名称」「活动评语」→「添加」照片→勾选幼儿，检查已选人数、占比及名单。
2. 点「发布活动」→「再看看」确认取消不发布；重新打开确认框，核对标题、涉及人数和不可修改提示后再确认发送。
3. 进「全部活动」找到刚发布的活动，对照文字、日期、照片；分别点前三张缩略图和超过三张的「+N」，记录是否能看大图。
4. 返回本周上传进度，核对所选幼儿；退出重进确认结果；测试活动的垃圾桶先取消，再在独立测试记录上确认删除，回查列表和周进度。

## 主要验收内容

- [ ] 发布前能辨认实际所选照片及目标幼儿，长文字和名单不被底部按钮遮挡。
- [ ] 空标题、缺必要内容或未选幼儿时给出具体提示；上传失败不得作为成功照片提交。
- [ ] 发布后列表与周进度反映真实结果；取消、重复点击和失败均不制造虚假成功。
- [ ] 单图、三图及多图的查看路径合理，照片比例和顺序正确。
- [ ] 涉及文字、图片、表格、弹层的检查至少留一份当前画面证据；如无旧版截图，记录“首次建立基线”，不判定与旧版完全一致。
- [ ] 意见、修改结果与人工复查分别落账；页面能打开不等于业务通过。

## 已知现状与边界

发布页照片区已显示实际所选图片，全部活动的每张缩略图和+N已接大图预览；本地后端已保存真实图片成品，详见B、C，等待人工复查。当前没有自动保存草稿、编辑或恢复入口；发布后只能删除重发。页面也不显示家长已查看人数，不能把它当作应恢复的旧统计。

这是代码阅读得出的审核线索，未在微信开发者工具中执行。本票的“希望行为”用于人工验收，不代表已经实现。

## 意见与修改记录

| 项目 | 记录 |
| --- | --- |
| 审核日期／教师／班级／学期 | 待填写 |
| 测试幼儿／记录标题 | 待填写 |
| 页面外观与操作 | 用户确认发布可见，并在真实图片与预览修复后回复“验证成功了”；本轮图片问题已通过 |
| 保存与回读结果 | 发布和真实图片回读已有用户确认；自动回归另覆盖后端重启后读取。删除取消／确认及页面周进度回落仍待人工审核 |
| 外部关联事项（如有） | 仅记录线索，不要求你验证家长端，不阻塞教师端审核 |
| 你的意见 | POST /moments返回422，随后提示字段相关错误，无法发布 |
| 希望改成什么 | 发布正常完成，失败时明确说明原因 |
| 截图或录屏 | 待填写，统一放 ../evidence/ |
| 助手修改说明 | SessionContext新增school_today，加载和发布前刷新后端日期；发布前加防重复锁，日期／照片等错误具体提示，失败保留输入 |
| 复查步骤与结果 | 重新编译→家园社共育→在园时光→发布活动，选照片与两名幼儿发布；再去全部活动及本周进度核对 |
| 最终结论 | 通过。用户回复“你需要我做的都通过了”，02号票关闭，进入03号票 |

反馈可以直接说：“票号—第几步—实际看到什么—希望改成什么”，我再补入本票。发现多个问题按 A、B、C 分项，各自记录修复和复查结果。操作不到时记录缺的入口或数据，不勾通过。

## Comments

DO-NOT-BUILD 核对（2026-09-11）：本票为人工审核草案，无新增功能施工，无冲突。后续修复需按实际修改范围重新核对。

## A：发布422已修（2026-09-12，用户已确认可发布并可见）

复现的响应为validation_failed，details.field=moment_date，rule=moment_date_not_after_school_today，原提示为“字段校验未通过”。原因是页面读取设备日期9月，再夹到当前测试学期末7月10日；测试后端的业务日期为4月25日。隐藏日期已是未来日，所以正确被服务端拒绝。

修复保留日期校验，改为GET /auth/session返回只读school_today。发布页加载及提交前刷新会话，以园所日期产生moment_date；不会把4月25日写死进页面，也不调用测试专用健康接口作为正式数据源。无学期或取不到日期有明确提示。增加momentPublishFailureText按字段与规则解释日期、照片和幼儿范围错误，未知错误保留通用提示，不拼undefined字段。

tools/probe-moment-publish.mjs已实跑：设备日期与后端不一致、旧隐藏日期在提交前刷新、取消不提交、无效照片422保留表单、实际上传图片→发布→DB照片/幼儿关联→周次数增加、双击只发布一次。探针自己创建的活动及文件已经清理，周次数恢复。前端8项、后端10项常规检查通过，API-CONTRACT v0.32与Swagger已同步，本机3860服务已重启。

这证明发布与引用落库；当前薄测试服务的媒体存储和渲染限制不因此视为通过。后续继续本票照片预览与查看流程。DO-NOT-BUILD复核：无冲突。

## B：feed图片大图预览（2026-09-12，待人工复查）

原因是只有+N角标绑定事件，前三张缩略图没有点击入口。每张缩略图现带moment_id与file_id绑定预览，从所点照片开始；+N从第4张开始。全组图片均提供给原生预览器，支持在同一活动中左右查看。

每次预览重新走既有GET /media/files/{file_id}/url取得有效地址，携带该活动宿主参数，不复用过期缩略图链接。保留file_id→URL对应关系，其他图片取址失败不改变当前照片；所点照片失败时提示，不悄悄打开另一张。没有新增接口或数据库改动。

6项回归检查通过：单图、点击指定缩略图、+N起始图、前序图片失败不偏移、所点照片不可用、原生预览失败反馈。前端8项检查通过。只读实测当前已发布活动取回2张图，所选大图URL返回200及image类型；原生放大和滑动仍需人工确认。

下一步：重新编译→家园社共育→在园时光→全部活动。分别点第1、第2张图，确认打开对应照片，再左右滑动、缩放和关闭返回；有4张以上时再点+N。确认后继续发布页选图预览和删除后周进度回落检查，不自动关闭02。

## C：上传显示假图（2026-09-12，已修，用户已确认通过）

用户上传 vertical.png 与 horizontal.png，却看到假图。根因是旧版薄测试后端仅记录上传元数据并丢弃实际文件，取图统一返回纯色占位 PNG；发布页还沿用渐变假缩略图。此前 A、B 只证明发布／引用、取址／预览调用成功，没有证明原始照片内容保留。

新上传已改为解析 multipart 实际字节、按既有规则处理 JPEG 并只持久化成品；db_file 使用现有 p2 本地存储，大小／哈希来自成品，无需迁移数据库。读取返回5分钟短链，每次取字节重验会话与宿主引用。发布页选图后显示实际本地缩略图；发布后的feed必须从服务端读取保存成品。

自动回归使用用户提供的两张 PNG，实际上传→发布→下载，核对竖图754×1093、横图1186×706以及完整成品字节；重启后再次读取一致。另验证方向／2000px缩放／metadata移除、无效图片拒绝、multipart篡改与长度不符、并发提交不重复、跨班拒绝、退出登录／删除活动后旧链失效。只创建并清理测试自身记录，没有重置幼儿评价或周进度。

旧版上传已丢失内容，不能从db_file恢复，必须重新上传。预置演示图片仍可能是占位图，不纳入本次新上传验收。

人工下一步：重新编译→家园社共育→在园时光→发布活动，重新添加这两张横竖图，确认选图区显示真实内容；发布后进全部活动，依次点两张图，检查内容、方向、滑动与缩放；退出重进再查看。通过后继续独立测试活动的删除及周进度回落检查，02暂不关闭。

## D：图片复查通过，进入删除与进度收尾

用户对上轮横竖图重新上传、真实内容及预览复查回复“验证成功了，下一个工作”。本轮图片问题记为通过，不再要求重复验收。该确认不扩展为尚未执行的删除、多图+N或其他页面验收。

下一步：家园社共育→在园时光，先记录测试活动涉及幼儿的本周次数；进入全部活动，找到自己发布的本周测试记录，点日期旁垃圾桶，先取消确认记录仍在，再确认删除。返回本周上传进度并退出重进，涉及幼儿次数应各减1，未涉及幼儿不变；回家园社主页核对完成度汇总，2→1由绿色实心变空心，3→2仍为实心。只操作可删除的测试活动。

若尚未审过4张及以上活动的+N入口，留作本票最后一项照片边界检查。删除与剩余边界确认后关闭02，再进入03亲子任务草稿、发布与完成情况。

## E：自动检查先行，缩小人工操作范围

用户询问哪些测试可以由助手完成，并要求人工部分写清点击动作及观察对象。后续由助手优先验证接口、数据库、计算边界及页面处理函数；原生按钮点击、弹窗排版、渲染与手势才交给用户。D段的手工计算步骤由以下自动回归替代，无需用户重复手算。

已实跑 tools/probe-moment-publish.mjs：调用真实页面删除处理函数，取消不发送DELETE，活动卡片／数据库／幼儿次数不变；确认发送一次DELETE，卡片移除，数据库活动、幼儿关联及照片引用删除。相关幼儿次数恢复，其他幼儿不变；周进度页与主页各重新加载两次，显示数据及三项汇总均恢复基线。临时数据已清理。这里模拟wx弹窗回调，没有实际操控微信开发者工具。

另实跑后端home-school-progress.test.mjs（事务回滚）：0／1未完成、3→2仍完成、2→1变未完成，平均完成和待提醒同步重算。6项大图预览处理函数测试也通过，含+N从第4张开始、全部照片顺序及失败提示；这些不替代原生预览器画面检查。

人工下一步仅检查画面：

1. 家园社共育→在园时光→全部活动，找到不需保留的、自己发布的本周测试活动；点日期旁垃圾桶。应出现“删除这条活动？”弹窗，正文标题对应当前活动，“取消／删除”两按钮可见且不被遮挡。
2. 点取消。弹窗关闭，原活动卡片仍在。
3. 再点垃圾桶→删除。出现“已删除”，该卡片消失；退出全部活动再进入，该卡片仍不出现。无报错、无卡住即可，不要求手算进度。
4. 如尚未检查多图入口，在一条4张以上测试活动点+N，应打开第4张，可左右滑到同一活动其余照片并正常关闭；若缺该样本，由助手另行准备，不要求用户为算法造数。

删除界面与多图实际操作确认后可关闭02，进入03；当前不把自动回归记成人工画面验收。

## F：最终验收

用户确认“你需要我做的都通过了”。结合此前发布、真实图片与预览确认，以及删除、数据库关联、周进度和汇总自动回归结果，02号票记为passed。不再要求重复以上检查；下一张为03亲子任务草稿、发布与家长完成情况，仍只审核教师端。

## G：补充移出成长册入口

用户补充：全部活动加入成长册后无法取消，应允许教师移出。原范围验收不作废，本项重新打开02；03表单审核保留待续。

已接既有教师素材删除接口。已收录卡片显示“移出成长册”，点击出现确认框；取消不写入，确认只删除收录与主题归类，原活动和照片、幼儿关联、本周次数均保留。移出后按钮回到“收进成长册”，允许重新加入。已锁定的本学期编册仍拒绝变更。

自动验证：12项交互回归（包含图片预览原有6项），新增取消／正确素材ID／重新加入／重复点击／锁定失败反馈／读失败不写／旧请求不覆盖；真实本地DB完整加入、取消移出、确认移出、回读、重新加入链路通过，源活动／照片／周次数不变。测试自身记录已清理，未修改用户既有收录。接口字段和数据库结构不变，仅新增页面调用入口。

人工下一步：重新编译→家园社共育→在园时光→全部活动，找到刚加入成长册的活动；点“移出成长册”→“取消”，应仍显示移出按钮；再点“移出成长册”→“移出”，应保留原活动卡片及照片，按钮变为“收进成长册”；退出重进后仍可重新加入。只看按钮、弹窗与画面，无需核算数据库或次数。通过后关闭本补充项并继续03亲子任务。
