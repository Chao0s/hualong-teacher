# 03: 亲子任务草稿、发布与家长完成情况

**What to build（本票交付）：** 教师发布明确的任务要求，重进可核对开始与截止日期、家长已读及完成状态，并正确结束任务。

**Blocked by：** None（可立即开始页面审核；写入、回读需 README 中的测试环境与样本就绪）。

**Status：** passed（草稿、发布、详情、结束及输入框复查均已确认；默认开始日期来源已修并自动验证）

**审核范围：** 仅教师端。家长账号操作和真实收发由外部负责，不作为本票通过前置；教师端已有的家长状态、回复和投稿仍需检查，缺样本交给助手准备或登记。

**拟确认后的执行状态：** ready-for-human-review

**优先级：** P0；P0 优先核对家园沟通，P1 紧随其后，P2 后续逐页扫查。

## 从哪里进入

底部「家园社共育」→「亲子任务」→「发布新任务」；返回后在「历史任务」点草稿继续编辑，点已发布任务进入「任务详情」。

## 准备什么

日常任务、社区任务各一条测试记录；准备未开始、进行中、已结束的已有记录，缺数据则记录待补。

## 按顺序操作

1. 选择「日常任务／社区任务」→ 填任务名称、背景、详情、开始日期与截止日期→「存为草稿」。
2. 从历史任务重开草稿核对回填，再「发布给家长」；测试「不设截止」及截止早于开始的提示。
3. 打开已发布任务，核对正文、时间、当前班级人数、已读与完成情况三列表格。
4. 用未开始／进行中／已结束样本核对状态和操作；在独立测试任务点「结束任务」，先取消再确认，重进核对不能重新开启。

## 主要验收内容

- [ ] 草稿与已发布任务入口不同，草稿内容回填完整，发布结果重进仍在。
- [ ] 类型、日期、正文与发布前一致；已读与已完成不能混为一项。
- [ ] 家长提交后教师状态应刷新；未读取到状态不冒充未读或已完成。
- [ ] 长任务说明、日期与多行幼儿表格可完整阅读。
- [ ] 涉及文字、图片、表格、弹层的检查至少留一份当前画面证据；如无旧版截图，记录“首次建立基线”，不判定与旧版完全一致。
- [ ] 意见、修改结果与人工复查分别落账；页面能打开不等于业务通过。

## 已知现状与边界

当前详情页只有提交状态，没有逐笔家长作品预览入口，代码记录G70缺口。不能写“点幼儿查看作品”作为现有步骤；需要阅读作品时另记需求。本票只检查教师可见状态；实际家庭收发属于外部事项。

这是代码阅读得出的审核线索，未在微信开发者工具中执行。本票的“希望行为”用于人工验收，不代表已经实现。

## 意见与修改记录

| 项目 | 记录 |
| --- | --- |
| 审核日期／教师／班级／学期 | 待填写 |
| 测试幼儿／记录标题 | 待填写 |
| 页面外观与操作 | 未开始／通过／发现问题 |
| 保存与回读结果 | 未开始／通过／发现问题／待接入／缺样本／不适用 |
| 外部关联事项（如有） | 仅记录线索，不要求你验证家长端，不阻塞教师端审核 |
| 你的意见 | 待填写 |
| 希望改成什么 | 待填写 |
| 截图或录屏 | 待填写，统一放 ../evidence/ |
| 助手修改说明 | 待修改；如涉及产品规则变更先列出冲突，不直接恢复旧原型 |
| 复查步骤与结果 | 修改后重走本票对应步骤并记录 |
| 最终结论 | 待人工确认；助手改完不自动视为审核通过 |

反馈可以直接说：“票号—第几步—实际看到什么—希望改成什么”，我再补入本票。发现多个问题按 A、B、C 分项，各自记录修复和复查结果。操作不到时记录缺的入口或数据，不勾通过。

## Comments

DO-NOT-BUILD 核对（2026-09-11）：本票为人工审核草案，无新增功能施工，无冲突。后续修复需按实际修改范围重新核对。

## A：开票与第一轮检查

当前安排：用户在02补充移出功能修复后明确“开始03”，本轮执行以下第一轮表单／草稿审核。02已通过部分保留，补充项未明确的人工结果不推定通过，也不阻塞本票。

02已由用户确认通过，03开始。已运行tools/probe-parent-task.mjs，119项通过、0失败：真实本地接口覆盖列表／详情、类型和计划时间、草稿新增／修改／回读、发布／结束状态约束、班级提交看板及跨班拒绝；临时测试记录已清理，任务60条、提交522条恢复开工基线。探针改为动态读取基线与在园人数，不回退数据库序列，不要求用户重置测试库。wx采用桩，不表示原生画面已验收。

第一轮只检查表单与草稿：

1. 家园社共育→亲子任务→发布新任务，选日常任务。填写名称“审核03—亲子阅读”；背景“培养亲子阅读习惯”；详情分两行“和孩子共读一本绘本。”及“请家长写一句孩子最喜欢的内容。”。观察字段可读，输入和键盘不挡操作。
2. 切到社区任务再切回日常任务。应只有类型选中态变化，刚才填写的名称、背景、详情均保留。
3. 为匹配本地测试学期，开始日期选2026-04-25，截止选2026-04-30；点存为草稿。回到亲子任务→历史任务，找到“审核03—亲子阅读”，应标为草稿；点开应进入编辑草稿，文字、换行、类型与两项日期正确回填。

本轮先不发布或结束；草稿画面确认后，再安排发布确认框、详情和已读／完成情况的人工检查。用户无需检查数据库或手算进度，无需登录家长端。

代码阅读线索：新建任务默认开始日期目前用设备日期Date.now()，与注释“园所今天”不一致；本轮显式使用测试学期日期，默认日期口径留在03继续核对，不将现状记为已通过。

## B：草稿通过，统一输入区域高度并进入发布

用户确认第一轮内容均成功，并反馈任务名称输入框高度与其他不一致。仅在本页将名称input、背景textarea、详情textarea统一为192rpx，明确覆盖原生控件不同的默认高度；保留名称单行、背景／详情多行及既有字数限制。无需改接口或数据库，实际显示随下一轮重新编译复查。

下一步：家园社共育→亲子任务→历史任务→审核03—亲子阅读（草稿），核对开始2026-04-25、截止2026-04-30。

1. 点发布给家长。弹窗应写明日常任务、当前标题、4月25日08:00开始、4月30日21:00截止，以及发布后不能修改的提示；按钮“再看看／确认发布”完整可见。先点再看看，应保留草稿及表单内容。
2. 再点发布给家长→确认发布。应提示已发布并返回任务列表；该任务不再标为草稿。点开应进入任务详情，而非编辑表单。
3. 详情正文、换行、类型和两项日期与草稿一致；下方“幼儿／已读／完成情况”三列表格及班级人数、已完成、未读可正常阅读。退出重进仍是详情。只检查教师端显示，不操作家长账号，不把家长收到通知当作本轮已验证。

本轮停在已发布详情，保留任务供后续结束任务检查；发布／状态／接口计算已有119项自动回归，本轮不要求用户核对数据库。

## C：发布通过，输入区域保持单行高度

用户明确任务名称、任务背景、任务详情均只需单行高度，并确认B段发布与详情检查成功。本页三个文字区域改为88rpx，行高40rpx；背景与详情保留textarea，仍可输入换行并在框内滚动，已有草稿正文不转换或截断。该要求替代B段192rpx高度。

下一步先重新编译，在发布新任务页目视确认三个文字框均为单行高度，无需另建任务。再返回家园社共育→亲子任务→历史任务→刚发布的测试任务，进行结束检查：

1. 点详情页底部“结束任务”，应显示“结束这个任务？”弹窗、未完成人数，以及结束后不能提交／不能重新开启的提示；点“再看看”，应保持已发布状态，结束按钮仍可见。
2. 再点“结束任务”并在弹窗确认。应提示“已结束”，状态变为已结束，结束按钮消失，显示“这个任务已经结束，不能重新开启。”。
3. 返回历史任务，再点开同一任务。列表与详情均应保持已结束，原正文与已读／完成表格仍可阅读，未完成不会因结束而变已完成。

结束状态约束已有真实接口自动回归，用户只验证界面和返回后的显示，不需要手算或登录家长端。此步通过后核对本票剩余事项；A段默认开始日期来源问题仍保留，尚未修复，不把本票提前记为完成。

## D：结束流程通过，三个输入框自动增高

用户确认其他操作没有问题，结束任务／取消／返回重进检查记为通过；补充输入框应随换行和自动折行增高。C段固定88rpx并内部滚动不满足该需求，已替换。

名称、背景、详情均使用auto-height textarea，保持现有字数限制和原值绑定；一行内容起步，换行、长文本自动折行、草稿回填交给原生排版增高，删短内容后随之收缩。外层.input保留边框／内边距和88rpx最小高度，内层无固定高度或留白，避免自动高度与边框盒模型相互裁切。不截断已有正文，不修改接口和数据库。

参考微信官方示例：https://github.com/wechat-miniprogram/miniprogram-demo/blob/master/miniprogram/packageComponent/pages/form/textarea/textarea.wxml 。Context7工具不可用，微信文档页访问失败，已读取官方示例源码核对auto-height用法。结构检查不等于微信原生渲染实测。

人工下一步：重新编译→家园社共育→亲子任务→发布新任务，分别在三个字段输入到自动折行，再按回车添加一行；应向下展开且不盖住下一字段。删回一行应缩回，保存草稿后重开长文本应完整展开。无需再次发布或结束，以上已通过部分不重复检查。默认开始日期来源仍是独立待办，不宣称03全部完成。

## E：最终验收与日期遗留项收尾

用户回复“没有了，开始下一个任务”，输入框复查通过。默认开始日期遗留项本轮已修：新建任务刷新GET /auth/session并使用school_today，编辑既有草稿仍使用保存日期；无法取得园所日期时提示重试，不回退电脑时间。

tools/probe-parent-evaluation.mjs只读实跑，将设备时间模拟为2099年，亲子任务新建日期仍与服务端school_today一致。结合此前119项真实接口检查及用户逐段确认，03记为passed；下一张04，03无需重复人工操作。
