**已定、未落：`db/spec/` 再加两份表，第 9 与第 10 步。** 按屏幕看 API 要 `screen-operations.tsv`（一个屏幕的一个操作一行，外键是契约现成的 `operationId`）与 `operation-eli10.tsv`（一个操作一段人话，「解释给十岁小孩听」）。前者是生成物（生成器在前端的 `tools/scan-wiring.mjs`），后者手工维护，**所以上面「手工维护的只有……」那句届时要多列一项**。两份各配一个检查文件，`check-all.mjs` 由八步变十步，**本文第 62、65、78、80、82、84 行的步数字样届时一起改** —— 注意 `check-consistency` 数的是 `DECISIONS.md` 的表数链（45 → 62），不是 `check-all` 的步数，**改这几处字样不会弄红任何一步**。第 80 行那句增步沿革保留，追加新的一笔。决定理由见 `../hualong-teacher/decision.md` 的 2026-09-11 一条；**在那两份表落地之前，本节其余文字与步数一律不动。**

