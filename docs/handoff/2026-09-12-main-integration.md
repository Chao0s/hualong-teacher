# main 合并记录与下一步

用户要求把当前教师端和配套后端推到main，并授权按合理实现解决冲突。

## 合并依据

- 教师端合入origin/master的80b6282及之前56个新增提交，再建立main。保留同事的登录页、教研首页、检测工具与文档更新。
- 后端合入origin/main的db219cb及之前21个新增提交。保留关联案例、关键词搜索、教研首页和栏目提交接口，以及新增缺口记录。
- 相册筛选合并双方实现：合法ID、本班范围、幼儿参与关系、游标绑定；查询内联重验幼儿当前班级。
- 交接冲突按用户最新验收结论处理，01已通过；不恢复旧的未解决状态。当前人工审核是12A／12B／12C。
- 接口登记表按同事新增的title_source列重新生成，保留亲子时光管理的三个跨服务读取。手写操作说明保留。
- 后端契约本地修订v0.31—v0.38调整为v0.33—v0.40，合并总计记在v0.41。原验收记录中的计数是各开发线当时的实测值。

## 验证范围

- 前端npm test共9组通过，含36项登录行为；另40项页面与服务回归通过。
- 后端常规检查10组通过；5项数据库／媒体回归通过。
- 隔离数据库全量加载DDL和seed，28项校验无违规；实际审核库增量迁移前后54个组件未改变。
- 新启动3863实例验证了版面完整回读、跨班拒绝、冻结后拒绝修改、错误保存保留旧版；10名幼儿相册与数据库一致，跨幼儿游标、非法ID和越界幼儿被拒绝。
- 家园总览10名幼儿及三项状态、汇总数字与数据库一致。Swagger静态文档构建通过，141 paths／172 operations／159 schemas。
- 跨三端扩展控件扫描仍报104处data-ui问题：55处来自审核报告／查看器，49处来自教师端页面；排版编辑器0处。未把扩展检查写成通过。输出留在后端本地.git/integration-ui.log。
- 寄语回归改为事务内建立无寄语前置，再回滚，避免人工新增的寄语使旧测试误报；不删除用户审核成果。

## 分支与本机文件

教师仓库GitHub默认分支仍为master，Pages工作流与部署环境也仍跟随master；本次目标是main，不改变既有默认分支和站点部署设置。后端默认分支为main。

源码、规格、迁移和审核脚本进入Git。真实图片文件、数据库内容、依赖、构建产物和开发者工具缓存保留本机，按忽略规则处理。其他机器复现图片数据需要运行相应准备脚本或重新上传。

## 下一步：人工审核12

入口：家园社共育→成长档案→成长册→编辑样板→点击“排版审核A”或“排版审核B”的栏目名称。

1. 12A：A第1页移动、缩放图片，保存手稿，退出重开，位置与大小应保留。
2. 12B：B修改两行文字、粗体／颜色及字号／对齐，保存手稿，退出重开，编辑区格式应保留。
3. 12C：A新增末尾空白页并保存，重开应保留；删除新增页再保存，页数应减少。

本批只保存手稿，不发布征集或锁定编册。详细步骤见[12批次](../../.scratch/manual-page-review-2026-09-11/batch-12-layout.md)。用户尚未确认这三组人工检查通过。
