# 下一轮交接：12号票新增栏目

本文件是本轮统一交接入口。12号票仍在审核中，13号票未开始。

## 已确认的产品规则

- “排版审核A／B”只是测试栏目名，不是新增功能。对用户统一称“新增栏目／编辑栏目”。
- 用户明确：教师新建的栏目即使已发布、收到家庭材料，也可以删除。
- ADR-0013以F19第六条为准：编册e1时允许d1／d2整栏删除；编册e2后拒绝。发布后的版面冻结不等于禁止整栏删除。
- 不要求用户审核家长端。家长实际收发不作为教师端验收前置。

## 本轮交付

1. 修复栏目名称输入框裁切，设定88rpx高度和水平内边距。
2. 修复文字对齐：原生editor逐段应用对齐，保留富文本、最新输入和末尾空行。左／中／右实际画面均验证。
3. 修复已发布新增栏目删除：同事务清理组件、家庭草稿／终局投稿、内容检查及文件引用；修复后续栏目插入位置，清理启用选择，递增编册revision。共享文件本体不由此动作删除。
4. 同步后端ADR摘要、契约v0.42、动作登记、覆盖账本、屏幕调用及Swagger说明；前端按钮、错误提示和防重复点击同步。

详细证据见[版面检查](2026-09-12-ticket-12-weapp-review.md)和[已发布栏目删除](2026-09-12-published-section-deletion.md)。

## 已验证及边界

- 前端常规9组通过；版面专项5项、删除专项4项通过。
- 后端常规10组通过；删除数据库专项6项通过，覆盖权限、锁定、清理、共享引用、重锚、失败回滚及重复删除。
- Swagger静态构建通过。契约141 paths／172 operations／159 schemas／135动作；缺口114条、8条BLOCKER，无增减。
- weapp：A图片位置尺寸保存重开一致；空白页3→4→3；B文字、样式、字号、对齐保存及画面通过。没有真实拖拽工具，editor也不支持工具直接键盘输入；这些调用了页面处理函数或原生editor加载，不能冒充手势和键盘选区验收。
- weapp实际删除专用已发布栏目59，数据库确认1个组件、2笔投稿、2条检查、2条引用清理；后继60重锚正确，随后经API清理60。现有栏目未删除，没有发家长通知。
- 三端扩展控件扫描此前104项标注问题仍保留，本轮未重跑，不以常规检查通过代替全量扫描通过。

## 当前样本与环境

- 15“排版审核A”、16“排版审核B”最后读取时均为已发布／征集中，未启用入册；不再使用它们做草稿改版。不要自动撤回或删除。
- 59／60只用于本轮删除验证，已清理。不要再次运行其验证脚本并把“找不到已清理样本”报成业务失败。
- 本地API：`http://127.0.0.1:3860/api/v1`，真实PostgreSQL测试库`hualong_test`，基准日2026-04-25。本轮已重启3860加载删除修复；临时3864已停止。下一轮先探活。
- 图片文件、数据库内容、会话及`.local.json`清单留本机，不随Git复制。截图、说明和验证脚本随仓库交付。脚本中样本的预期值是本轮检查时点，不作为永久全局回归。
- 项目根目录为`D:/hualong-teacher`。weapp使用`preserveProjectRoot: true`。导航后等待目标节点出现；仅固定等待可能先读到上一页。
- 后端无新增表结构，不需要为本轮重灌或迁移数据库。

## Git交付

前后端均使用`main`。教师端已快进合入远端4个新增提交至`3c4515a`，保留启动脚本、项目约定及Pages改为main触发的变更。

后端本轮提交为`8a3bf18`，已推GitHub main。推送顺序为后端再教师端。Pages构建会读取GitHub后端契约；本轮推教师端main会触发构建。已通过GitHub API确认默认分支和Pages来源分支均为main。生产API未部署。教师端提交号和CI结果由本轮最终回复记录。

## 下一步：由助手执行

第一步读取教师端“栏目投稿”页面及`GET /teacher/growth-book/sections/{section_id}/submissions`（教师读取班级新增栏目家庭终局投稿）的现有返回和权限规则。该接口已经存在，页面尚未接入；不要再写“契约没有读取接口”。

入口：家园社共育 → 成长档案 → 成长册 → 编辑样板 → 点击一个已发布的新增栏目名称。

继续完成投稿内容展示与照片查看、全部／已交齐／未交齐筛选、征集开关及提醒结果。先准备专用本地样本并自动验证数据库与接口，再安排最多3组互不依赖的人工画面检查。提醒会建App内通知，实际发送前遵守会话授权范围。

本轮删除功能已通过工具点击及数据库核对，不要求用户重复测试。此前拖拽和键盘选区仍无用户明确验收结论；如需补查，用新的专用草稿，保留已发布15／16。收到明确验收再关闭对应检查项，不能因用户发布了栏目就推断整票通过。
