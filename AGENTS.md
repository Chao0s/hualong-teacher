# 项目协作约定

完整工程约定见 [CLAUDE.md](CLAUDE.md)。修改业务前读取相关决策、规格及目标仓库的工作约定；用户在当前会话中确认的新要求优先于旧文档。

## 每次回复必须明确下一步

- 每次最终回复都要明确告诉用户下一步做什么，不能只说“已完成”“已修改”或“测试通过”。
- 区分下一步由用户还是助手执行。人工审核要给出具体页面、完整点击路径、按钮和本轮检查重点；不要只报源码目录名。
- 用户确认当前改动检查通过后，及时记录验收结果，说明当前ticket是否完成；完成则指出下一张ticket和第一个检查动作。
- 如果仍有问题，列明剩余事项及下一步优先处理哪项；不要把已解决事项重复当作待办，也不要为用户增加已明确排除的家长端审核。
- 没有待办时直接说明工作已完成、无需继续操作，不虚构下一项任务。
- 提到“已接接口”时说明实际数据来源及验证范围，区分本地测试数据库、页面固定样例和纯前端交互；不要把UI状态保存说成新增数据库接口。

## 人工审核按批次开展

- 用户希望加快节奏：默认每轮安排3组互不依赖的人工检查，可跨ticket，不必每做一步就等待助手回复。
- 助手先完成可自动验证的接口、数据库、计算与回读检查，准备具体样本；人工只查按钮、弹窗、输入、图片和实际画面。
- 每组写明票号、入口、样本、操作与预期；组内有先后依赖的保存／发布按顺序做。反馈允许按“05通过；06第2步问题；07通过”批量提交。
- 一组遇到问题先登记，继续独立组；已知未接入入口列为开发待办，不要求用户重复点击证明。同组通过不冒充整票通过，未审核部分继续保留。

## Context7 文档查询

当用户询问库、框架、SDK、公共API、CLI工具或云服务的用法、配置、版本迁移、相关调试时，优先使用Context7 MCP获取当前文档，即使熟悉该技术也要查询。

1. 用户未提供 `/org/project` 形式的精确ID时，先以库名和具体问题调用 `resolve-library-id`。
2. 按名称、描述相关性、片段数量、来源信誉和评分选择库；指定版本时使用相应版本。结果不相关则调整名称或查询。
3. 对每个独立概念分别调用 `query-docs`，避免把互不相关的问题混在同一查询中。
4. 根据实际取得的文档回答；工具不可用时明确说明，不声称已查询。

不因普通重构、从零写脚本、业务逻辑调试、代码审查或一般编程概念而调用Context7。
